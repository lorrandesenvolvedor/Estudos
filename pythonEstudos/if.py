idade = 19
if idade > 18:
    print ("você e maior de idade")
else:
    print ("você não e maior de idade ainda!")
    
# Neste cód vemos como usar o if para verificar ce uma idade e maior ou menor, isso começa com uma variável simples; idade = 19, logo apos entra o if idade > 18: que o > indica que o número dogitado no caso o 19 deve ser maior cask vc ponha algo menor ele dira que e menor, e dps usamos o print ("maior") e logo apos o else que significa uma segunda opção no caso ce o número nao for maior ele dira que e menor usando else: print ("menor") desta forma.
